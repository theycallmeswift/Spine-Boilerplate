@import './mixin'

.stack > *:not(.active)
  display: none

.stack .sidebar
  display: block

.tabs
  list-style: none;
  margin: 0; padding: 0;
  overflow: hidden;

  li
    float: left;
    padding: 5px 10px;

  li.active
    border: 1px solid #000;
